/**
 * @description show the mentee login by default with a toggle switch to sshow the mentor login
 */

import { Box, Stack, Switch } from '@chakra-ui/react';
import { useState } from 'react';
import MentorLogin from './MentorLogin';
import MenteeLogin from './MenteeLogin';

const ROLE = { MENTOR: 'MENTOR', MENTEE: 'MENTEE' };

export const Signin = () => {
  // const [acnewRole => setActiveRole(newRole);

  return (
   'Login'
  );
};
